<!DOCTYPE html>
<div class="container">
<html>
<head>
  <title>FW</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

<div class="navbar">
  <div class="navbar-inner" style="-webkit-border-radius: 0; -moz-border-radius: 0; border-radius: 0;">
    <div class="container">
		<ul class="nav">
			<li>
				<a class="brand" href="<%= root_path %>">    FW    </a>
			</li>
			<ul class="nav pull-right">
				<% if user_signed_in? %>
					<li> Hi, <%= current_user.email %> </li>
					<li>
						<%= link_to "sign out", destroy_user_session_path, method: :delete %>
					</li>
				<% else %>
					<li>
						<a href="<%= new_user_session_path %>">log in</a>
					</li>
					<li>
						<a href="<%= new_user_registration_path %>">sign up</a>
					</li>
				<% end%>
				<li>
					<a href="<%=new_outfit_path %>">add a new outfit</a>
				</li>
			</ul>
		</ul>
	</div>
</div>
</head>
<body>
<br>
<br>
<%= yield %>

</body>
</html>

</div>
